{
  "name": "human-website",
  "version": "0.1.0",
  "description": "Human, an Education Collective — bilingual website (EN + ZH).",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18.17.1"
  },
  "scripts": {
    "dev": "node scripts/sandbox.mjs dev",
    "dev:raw": "astro dev",
    "start": "node scripts/sandbox.mjs dev",
    "build": "node scripts/sandbox.mjs build",
    "build:raw": "astro build",
    "preview": "node scripts/sandbox.mjs preview",
    "check": "node scripts/sandbox.mjs check",
    "check:raw": "astro check",
    "humanize": "node scripts/humanize.mjs",
    "humanize:check": "node scripts/humanize.mjs --check",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "gray-matter": "^4.0.3",
    "typescript": "^5.7.0"
  }
}
